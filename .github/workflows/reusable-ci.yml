name: Reusable CI

on:
  workflow_call:
    inputs:
      app_name:
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Application = ${{ inputs.app_name }}"
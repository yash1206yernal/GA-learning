name: Env Context Demo

on:
  workflow_dispatch:

env:
  APP_NAME: GitHubActionsDemo

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "APP = ${{ env.APP_NAME }}"

      - run: |
          echo "Variable = ${{ vars.APP_NAME }}"      
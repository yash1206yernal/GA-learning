name: Contains Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Check Repository
        if: ${{ contains(github.repository, 'GA-learning') }}
        run: echo "Repository Match Found"
name: StartsWith Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: StartsWith Demo
        if: startsWith(github.ref, 'refs/heads/')
        run: echo "This workflow is running from a branch"
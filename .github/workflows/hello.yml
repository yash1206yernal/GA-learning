name: Boolean Input Demo

on:
  workflow_dispatch:
    inputs:
      run_tests:
        description: "Run Tests?"
        required: true
        type: boolean

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Run Tests = ${{ inputs.run_tests }}"
name: Redis Service Demo

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      redis:
        image: redis

    steps:
      - name: Verify Redis Container
        run: docker ps
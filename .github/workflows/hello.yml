name: GitHub Context

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Repository
        run: echo "${{ github.repository }}"

      - name: Actor
        run: echo "${{ github.actor }}"

      - name: Branch
        run: echo "${{ github.ref }}"

      - name: Workflow
        run: echo "${{ github.workflow }}"
name: AND Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: AND Condition
        if: github.ref == 'refs/heads/main' && github.actor == 'yash1206yernal'
        run: echo "Both conditions are true!"
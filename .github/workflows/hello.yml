name: Container Job Demo

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    container:
      image: node:20

    steps:
      - name: Show Node Version
        run: node --version

      - name: Show OS
        run: cat /etc/os-release
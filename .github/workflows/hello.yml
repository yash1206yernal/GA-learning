name: Upload Artifact Demo

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Create File
        run: echo "Hello Artifact" > app.txt

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-file
          path: app.txt
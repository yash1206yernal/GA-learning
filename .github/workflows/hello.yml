name: Day 7 First Action

on:
  workflow_dispatch:

jobs:

  demo:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: List Repository Files
        run: ls -la

      - name: Show Current Directory
        run: pwd
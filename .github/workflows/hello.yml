name: OR Demo

on:
  workflow_dispatch:

jobs:
 demo:
    runs-on: ubuntu-latest

    steps:
      - name: OR Condition
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev'
        run: echo "Main or Dev branch"
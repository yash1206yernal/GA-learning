name: EndsWith Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: EndsWith Demo
        if: endsWith(github.ref, 'main')
        run: echo "The branch ends with 'main'"
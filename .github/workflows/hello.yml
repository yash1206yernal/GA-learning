name: Matrix Demo

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        version: [1, 2, 3]

    steps:
      - name: Print Version
        run: echo "Version = ${{ matrix.version }}"
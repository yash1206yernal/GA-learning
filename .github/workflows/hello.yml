name: Secret Environment Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Use Secret as Environment Variable
        env:
          PASSWORD: ${{ secrets.MY_SECRET }}

        run: |
          echo "Password is: $PASSWORD"
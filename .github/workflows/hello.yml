name: Runner Context Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "OS = ${{ runner.os }}"
          echo "Architecture = ${{ runner.arch }}"
          echo "Temp = ${{ runner.temp }}"
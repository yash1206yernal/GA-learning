name: Include Demo

on:
  workflow_dispatch:

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [18,20]

        include:
          - os: windows-latest
            node: 22

    runs-on: ${{ matrix.os }}

    steps:
      - run: |
          echo "OS=${{ matrix.os }}"
          echo "Node=${{ matrix.node }}"
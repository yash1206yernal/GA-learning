name: Least Privilege Demo

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Explain Permissions
        run: |
          echo "Following the Principle of Least Privilege"
          echo "This workflow only needs read access."
          echo "No write permissions have been granted."
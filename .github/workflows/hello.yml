name: Day 6 Variables & Secrets

on:
  workflow_dispatch:

env:
  APP_NAME: MyApplication

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print App Name
        run: echo $APP_NAME

      - name: Print Secret
        run: echo ${{ secrets.MY_SECRET }}
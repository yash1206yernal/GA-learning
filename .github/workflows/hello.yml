name: NOT Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: NOT Condition
        if: ${{ github.ref != 'refs/heads/dev' }}
        run: echo "This is NOT the dev branch"
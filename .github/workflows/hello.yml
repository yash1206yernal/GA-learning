name: Number Input Demo

on:
  workflow_dispatch:
    inputs:
      replicas:
        description: "Number of replicas"
        required: true
        type: number

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Replicas = ${{ inputs.replicas }}"
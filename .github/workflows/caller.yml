name: Caller Workflow

on:
  workflow_dispatch:

jobs:
  call-workflow:
    uses: ./.github/workflows/reusable-ci.yml

    with:
      app_name: GitHubActionsDemo

    secrets:
      app_secret: ${{ secrets.MY_SECRET }}